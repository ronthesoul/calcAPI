apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calcapi.fullname" . }}-test-connection"
  labels:
    {{- include "calcapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calcapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
